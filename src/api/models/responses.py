# TODO: Make sure all responses follow a common reponseModel like this.
#  Similar structure to HTTPException.
#  We may need to make our own HTTPException if we are to standardize responses.
# def responseModel(data, code: int, message):
#     return {
#         "data": data,
#         "code": code,
#         "message": message
#     }
